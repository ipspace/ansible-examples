#
# Get configuration from managed hosts (change the included
# playbook to switch between various retrieval mathods)
# and store it into Git repo in branch Actual
#
---
- import_playbook: gc_napalm.yml
- import_playbook: git_commit.yml
