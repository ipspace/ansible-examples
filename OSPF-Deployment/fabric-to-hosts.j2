#
# Auto-generated inventory file
#
{% for node in nodes %}
{{ node.name }} ansible_host={{ node.mgmt }} ansible_os={{node.os|default(common.os)
  }} ansible_user={{node.user|default(common.user)|default("missing")
  }} ansible_pass={{node.password|default(common.password)|default("missing")}}
{% endfor %}
